# ── Core Streaming ──────────────────────────────────────────
pyspark==3.5.0
confluent-kafka==2.4.0

# ── ML ──────────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0      # SMOTE oversampling
joblib>=1.3.0
dill>=0.3.8

# ── MLflow (Experiment Tracking & Model Registry) ──────────
mlflow>=2.14.0

# ── Airflow (installed separately — see README) ─────────────
# apache-airflow==2.9.2

# ── Drift Detection ─────────────────────────────────────────
evidently>=0.4.30
scipy>=1.13.0

# ── LLM Agent ───────────────────────────────────────────────
langchain>=0.2.0
langchain-google-genai>=1.0.0
pydantic>=2.0.0

# ── Data / Utils ────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
python-dotenv>=1.0.0
requests>=2.31.0
